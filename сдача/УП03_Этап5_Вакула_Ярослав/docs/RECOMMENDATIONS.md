Настроить автоматическое создание backup по расписанию (cron / планировщик Windows).

Внедрить проверку секретов в CI/CD (например, git-secrets или trufflehog).

Заменить SQLite на PostgreSQL для production-среды.

Добавить HTTPS (Let's Encrypt) при развёртывании на реальном сервере.

Регулярно выполнять pip-audit и обновлять зависимости.